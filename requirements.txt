# Runtime dependencies, mirroring [project.dependencies] in pyproject.toml,
# which is the source of truth. Prefer `pip install euler-eval`, or
# `pip install -e ".[dev]"` for development.
numpy>=1.21.0
scipy>=1.7.0
Pillow>=8.0.0
torch>=1.9.0
torchvision>=0.10.0
opencv-python-headless>=4.5.0
lpips>=0.1.4
torchmetrics>=1.0
tqdm>=4.62.0
euler-loading>=2.15
ds-crawler>=2.10
euler-metric-naming>=0.2
